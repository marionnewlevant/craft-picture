{# passed img and transformedImages and attrs #}

<img
  {% if transformedImages|length > 1 %}
    srcset="
    {% for i in transformedImages %}
      {{ i.url }} {{ i.width }}w{% if not loop.last %},{% endif %}
    {% endfor %}
    "
  {% endif %}
  {% if img.sizes ?? false %}
    sizes="{{ img.sizes }}"
  {% endif %}
  src="{{ transformedImages[0].url ?? '' }}"
  {% for key, value in attrs %}
    {{ key }}="{{ value }}"
  {% endfor %}
/>
